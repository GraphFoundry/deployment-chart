apiVersion: v1
kind: Service
metadata:
  name: integrated-dashboard-nodeport
  namespace: graph-foundry-system
spec:
  type: NodePort
  selector:
    app: integrated-dashboard
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30050
