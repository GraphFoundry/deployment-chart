apiVersion: v1
kind: Service
metadata:
  name: integrated-dashboard
  namespace: graph-foundry-system
  labels:
    app: integrated-dashboard
spec:
  type: ClusterIP
  selector:
    app: integrated-dashboard
  ports:
    - name: frontend
      port: 80
      targetPort: 80
